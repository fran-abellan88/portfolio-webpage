## Python Code Quality

- Comply with Flake8(W293), Flake8(W291), Flake8(W292)
- Add type hints to all code
- Remove unused imports
- Ensure f-strings contain placeholders — never bare `f""` strings (Flake8 F541)

## Output Formatting

- Wrap PR descriptions, commit messages, and structured markdown in a code block (` ```markdown ``` `) for easy copying
- Never output raw markdown that renders in the terminal
